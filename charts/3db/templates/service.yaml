apiVersion: v1
kind: Service
metadata:
  name: {{ template "3db.fullname" . }}
  labels:
    app: {{ template "3db.name" . }}
    chart: {{ template "3db.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "3db.name" . }}
    release: {{ .Release.Name }}
